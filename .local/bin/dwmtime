#!/bin/sh
while true; do
	FILE=$(cat $HOME/.local/share/notes/temp)
	NEWS=$(cat $HOME/.local/share/dwmtime/news)
	BATT=$(cat $HOME/.local/share/dwmtime/batt)
	xsetroot -name "$BATT$FILE$NEWS $(date '+%H:%M ')"
	sleep 30
done
