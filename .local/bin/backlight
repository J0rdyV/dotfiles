#! /bin/sh
brfile='/sys/class/backlight/amdgpu_bl1/brightness'
case "$1" in
	+)
		amount=$(sed -e 's/$/ 3000 + p/' $brfile | dc)
		echo $amount | sudo tee $brfile
		;;
	-)
		amount=$(sed -e 's/$/ 3000 - p/' $brfile | dc)
		echo $amount | sudo tee $brfile
		;;
	*) sudo echo $1 > $brfile;;
esac
